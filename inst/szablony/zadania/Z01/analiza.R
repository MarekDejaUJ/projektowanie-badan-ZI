# Z01 — {{ID}}. Ten skrypt jest gotowy do uruchomienia.
library(badaniaZI)

# W Z01 nie zmieniaj parametrów. Uruchom cały plik od pierwszej linii.
parametry <- list(tryb = "orientacja")

uruchom_analize("Z01", katalog = ".", parametry = parametry)

